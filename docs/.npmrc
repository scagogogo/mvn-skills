# vitepress-plugin-mermaid declares a peer dep on vitepress ^1.x, but this site
# runs vitepress 2.0-alpha. The plugin works fine against 2.0-alpha at runtime,
# so relax peer-dep resolution instead of pinning vitepress back to 1.x.
legacy-peer-deps=true
